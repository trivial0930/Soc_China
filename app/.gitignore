# runtime DB + uploaded evidence (regenerated by seed.py / uplink)
data/runtime/*
!data/runtime/.gitkeep
# seed demo images are caught by the repo-root *.jpg rule; force-include them
!data/seed/evidence/*.jpg
